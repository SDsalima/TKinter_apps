import tkinter as tk


root=tk.Tk()
tk.Label(root, text="lableTOP", bg="red").pack(side="top",fill="both", expand=True)
tk.Label(root, text="lableTOP", bg="red").pack(side="top",fill="both", expand=True)
tk.Label(root, text="lableLeft", background="green").pack(side="left", fill="both", expand=True)

root.mainloop()